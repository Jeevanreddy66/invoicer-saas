export type InputDetails = {
  name: string;
  placeholder: string;
};

export type InvoiceInputDetails = {
  label: string;
  id: string;
  type: string;
  name: string;
  placeholder?: string;
};

export type InvoiceTableDetails = {
  name: string;
  placeholder: string;
  className: string;
};
