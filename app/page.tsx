"use client";

import { FC } from "react";
import { HeroBanner } from "@/components";

const HomePage: FC = () => {
  return (
    <main>
      <HeroBanner />
    </main>
  );
};

export default HomePage;
